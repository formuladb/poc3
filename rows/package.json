{
  "name": "@formuladb/object-store",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch index.ts --exitcrash --exec 'ts-node' index.ts",
    "build": "tsc",
    "test": "cp -ar core /core && mkdir -p /tenants/default && cp -ar apps/* /tenants/default/ && make -f scripts/migrate.mk"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mime": "^2.4.6",
    "minio": "^7.0.16",
    "nodemon": "^2.0.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/minio": "^7.0.6",
    "@types/node": "^14.14.8",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
